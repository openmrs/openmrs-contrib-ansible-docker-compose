DESTROY_VOLUMES=false
# First boot (liquibase install + demo data, all Envers-audited) can exceed
# 30 minutes; the backend healthcheck only reports healthy once startup
# completes (/health/started).
HEALTH_CHECK_TIMEOUT=3600